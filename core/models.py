# Domain models (availability slots, activities, interest, access) will live
# here. The shareable calendar link lives on the User instead of a separate
# Calendar model — see SOLUTION.md for that decision.
